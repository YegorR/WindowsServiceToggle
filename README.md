# WindowsServiceToggle
Python script for easy starting or stopping a service in Windows OS
pywin32 package is needed

To change service the script works with, change the variable SERVICE_NAME (line 1)
